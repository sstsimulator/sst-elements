Router model
    bw				Port bandwidth
    hop_delay			Router latency

    debug			Level of verbosity
    component_name		Name of router
    num_ports			Number of ports on this router
    frequency			Needed for power model



Network NIC model
    NetNIClatency		Latency from user level to output link
    NetNICbandwidth		Bandwidth through the NIC
    NetNICgap			Minimum gap between messages

NoC NIC model
    NoCNIClatency		Latency from user level to output link
    NoCNICbandwidth		Bandwidth through the NIC
    NoCNICgap			Minimum gap between messages
