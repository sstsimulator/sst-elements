An example xml using DRAMSimC and DRAMSimTrace is dram.xml

1. Create links for the system.ini file and dramsim configs folder
2. ln -s <dramsim_path>/ini .
3. ln -s <dramsim_path>/system.ini.example

4. Run the example
    Make sure both the DRAMSimTrace component and DRAMSimC component of sst is built

    LD_LIBRARY_PATH=<dramsim_path> sst.x dram.xml > /dev/null
